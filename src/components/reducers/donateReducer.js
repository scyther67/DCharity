const initialState = {
    donateTo:''
}

export default function (state = initialState, action) {
    switch (action.type) {
        // case GET_ALLCHARITIES:
        default:
            return state;
    }
}